{
  "name": "claimdrop-server",
  "version": "1.0.0",
  "description": "Backend API for ClaimDrop",
  "private": true,
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "echo 'No build step necessary for ES Modules' && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "nodemailer": "^7.0.12",
    "twilio": "^5.11.2"
  },
  "devDependencies": {
    "supabase": "^2.72.8"
  },
  "overrides": {
    "tar": "7.5.6"
  }
}
